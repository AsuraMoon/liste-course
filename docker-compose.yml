version: "3.8"
services:
  app:
    build:
      context: .
      # active BuildKit secret only if DOCKER_BUILDKIT=1 is set when building
    image: liste-course:local
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
